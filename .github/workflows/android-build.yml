name: Android Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential nlohmann-json3-dev glm-dev
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle
    
    - name: Build Console Test
      run: |
        mkdir -p build
        cd build
        cmake ..
        make -j4
        ls -la bin/
        ./bin/polymaster_test
    
    - name: Upload Console Test
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: polymaster_test
        path: build/bin/polymaster_test
        retention-days: 30
